syntax = "proto3";

import 'model/integration.proto';

package livil.model;

message User {
  string id = 1;
  string arbitrary_id = 2;
  uint64 created_at = 4;
  string token = 5;
  repeated Integration integrations = 6;
  string environment_guid = 7;
}
